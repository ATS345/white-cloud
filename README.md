# 木鱼游戏平台

## 项目简介
木鱼游戏平台是一个基于微服务架构的游戏商店平台，提供游戏浏览、购买、下载、评论等功能。

## 功能特性

### 核心功能
- 🔐 用户认证与授权
- 🎮 游戏浏览、搜索、分类
- 🛒 游戏购买与支付
- 📥 游戏下载与管理
- ⭐ 游戏评分与评论
- 👨‍💼 开发者中心
- 📊 数据分析与报表

### 微服务架构
- **主后端服务**：处理核心业务逻辑和API网关
- **支付服务**：处理支付流程和订单管理
- **评论服务**：处理游戏评论和评分
- **通知服务**：处理用户通知和消息

## 技术栈

### 后端技术
- **Node.js** + **Express.js**：Web框架
- **Sequelize**：ORM框架
- **Redis**：缓存服务
- **MySQL**：数据库
- **JWT**：身份认证
- **Docker**：容器化部署
- **Kubernetes**：容器编排

### 前端技术
- **React.js**：前端框架
- **Redux**：状态管理
- **React Router**：路由管理
- **Axios**：HTTP客户端
- **Material-UI**：UI组件库

### 测试与质量
- **Jest** + **Supertest**：单元测试与集成测试
- **ESLint**：代码质量检查
- **Prometheus** + **Grafana**：监控与可视化

## 快速开始

### 环境要求
- Node.js 18+ 或 20+
- MySQL 8.0+
- Redis 6.0+
- Docker (可选)
- Kubernetes (可选)

### 本地开发

1. **克隆项目**
```bash
git clone https://github.com/your-username/muyu-game-platform.git
cd muyu-game-platform
```

2. **配置环境变量**
```bash
# 后端环境配置
cp backend/.env.example backend/.env
# 编辑backend/.env文件，配置数据库、Redis等信息

# 前端环境配置
cp frontend/.env.example frontend/.env
# 编辑frontend/.env文件，配置API地址等信息
```

3. **安装依赖**
```bash
# 安装后端依赖
cd backend
npm install

# 安装前端依赖
cd ../frontend
npm install
```

4. **启动服务**
```bash
# 启动后端服务
cd ../backend
npm run dev

# 启动前端服务
cd ../frontend
npm run dev
```

5. **访问应用**
- 前端：http://localhost:3000
- 后端API：http://localhost:5000
- API文档：http://localhost:5000/api/v1/docs

### 使用Docker Compose

1. **启动所有服务**
```bash
docker-compose up -d
```

2. **停止所有服务**
```bash
docker-compose down
```

## 目录结构

```
.
├── backend/                    # 后端代码
│   ├── config/                 # 配置文件
│   ├── controllers/            # 控制器
│   ├── middleware/             # 中间件
│   ├── models/                 # 数据模型
│   ├── routes/                 # 路由配置
│   ├── services/               # 业务逻辑
│   ├── utils/                  # 工具函数
│   ├── tests/                  # 测试文件
│   ├── start.js                # 应用入口
│   └── package.json            # 依赖配置
├── frontend/                   # 前端代码
│   ├── public/                 # 静态资源
│   ├── src/                    # 源代码
│   └── package.json            # 依赖配置
├── microservices/              # 微服务
│   ├── payment-service/        # 支付服务
│   ├── review-service/         # 评论服务
│   └── notification-service/   # 通知服务
├── docs/                       # 项目文档
├── k8s/                        # Kubernetes配置
├── docker-compose.yml          # Docker Compose配置
└── README.md                   # 项目说明
```

## API文档

### 认证API
- `POST /api/v1/auth/register` - 用户注册
- `POST /api/v1/auth/login` - 用户登录
- `POST /api/v1/auth/logout` - 用户登出

### 游戏API
- `GET /api/v1/games` - 获取游戏列表
- `GET /api/v1/games/:id` - 获取游戏详情
- `POST /api/v1/games` - 创建游戏（开发者）
- `PUT /api/v1/games/:id` - 更新游戏（开发者）

### 支付API
- `POST /api/v1/orders` - 创建订单
- `GET /api/v1/orders/:id` - 获取订单详情
- `POST /api/v1/payments` - 发起支付

### 评论API
- `GET /api/v1/reviews/game/:gameId` - 获取游戏评论
- `POST /api/v1/reviews` - 创建评论
- `PUT /api/v1/reviews/:id` - 更新评论

### 通知API
- `GET /api/v1/notifications/user/:userId` - 获取用户通知
- `PUT /api/v1/notifications/:id/read` - 标记通知为已读

## 测试

### 运行单元测试
```bash
cd backend
npm test -- --testPathPattern=tests/unit
```

### 运行集成测试
```bash
cd backend
npm test -- --testPathPattern=tests/integration
```

### 运行所有测试
```bash
cd backend
npm test
```

## CI/CD

项目使用GitHub Actions实现CI/CD流水线，包括：
- 代码提交触发自动测试
- 主分支合并触发自动构建和部署
- Docker镜像构建和推送
- 安全扫描

## 监控与日志

### Prometheus监控
- 所有服务暴露 `/metrics` 端点用于Prometheus采集
- 监控指标包括：请求次数、响应时间、错误率等

### Grafana可视化
- 提供预配置的Grafana仪表盘
- 支持实时监控和历史数据查询

### 日志管理
- 使用Winston进行日志记录
- 支持多环境日志配置
- 支持日志文件旋转

## 部署

### 本地开发环境
- 使用 `npm run dev` 启动开发服务

### 测试环境
- 使用Docker Compose部署
- 配置测试数据

### 生产环境
- 使用Kubernetes部署
- 实现高可用和负载均衡
- 配置自动伸缩

## 开发规范

### 代码风格
- 后端：ESLint + Airbnb规则
- 前端：ESLint + React推荐规则

### 提交规范
- 采用Conventional Commits规范
- 提交信息格式：`<type>(<scope>): <description>`

### 分支策略
- `main`：主分支，用于生产环境
- `develop`：开发分支，用于集成测试
- `feature/*`：功能分支，用于开发新功能
- `bugfix/*`：修复分支，用于修复bug

## 贡献指南

1. Fork项目
2. 创建功能分支
3. 提交代码
4. 运行测试
5. 提交Pull Request

## 许可证

MIT License

## 联系方式

- 项目维护者：木鱼游戏团队
- 邮箱：contact@muyugame.com
- 文档：https://docs.muyugame.com

## 版本历史

- v1.0.0：初始版本，包含核心功能
- v1.1.0：添加微服务架构
- v1.2.0：添加监控和日志
- v1.3.0：优化性能和安全性

## 致谢

感谢所有为项目做出贡献的开发者和社区成员！
